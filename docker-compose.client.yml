version: '2'
services:

    #
    # mender-client
    #
    mender-client:
        image: mendersoftware/mender-client-qemu:update_modules_demo
        networks:
            - mender
        stdin_open: true
        tty: true
        privileged: true
